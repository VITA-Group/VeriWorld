"""DropToTarget — deform a GPU cloth surface to roll a ball into a target.

Shared task assets (generate_params, setup_observe, task.md, api.md,
example.py, lean_verify/) live at this level. Per-ablation harnesses
live in sibling subfolders (see ``visual/``).
"""
