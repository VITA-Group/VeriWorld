"""Per-ablation entry point stub.

Real ablations follow the pattern of ``veriworld/scripts/run_parallel``
— invoked via ``python -m <dotted.module>``. This stub exists only to
show the folder shape expected at Layer 3.
"""

if __name__ == "__main__":
    raise SystemExit(
        "This is a harness-example stub. Copy a real __main__.py from "
        "veriworld/benchmark/**/ into your harness when bootstrapping."
    )
